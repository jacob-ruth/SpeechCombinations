package transcribe

import io.humble.video
import java.io.File
class VideoConverter(filename: String) {
	// def play():Unit = {
	// 	val demuxer =  Demuxer.make();

	// 	demuxer.open(filename, null, false, true, null, null)
	// 	val numStreams = demuxer.getNumStreams()
	// }
}